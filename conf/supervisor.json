{
        "commands": ["sleep 1 && exit 0", "sleep 5 && exit 0", "sleep 1 && exit 1", "sleep 10 && exit 1", "if [ -f lock ]; then exit 1; fi; sleep 10 && touch lock && exit 1"],
        "wait": "1",
        "interval": "1",
        "retry": "3"
}